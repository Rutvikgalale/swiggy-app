apiVersion: v1
kind: Service
metadata:
  name: swiggy-app-service
  labels:
    app: swiggy-app
spec:
  type: LoadBalancer
  selector:
    app: swiggy-app
  ports:
    - targetPort: 3000
      port: 80
